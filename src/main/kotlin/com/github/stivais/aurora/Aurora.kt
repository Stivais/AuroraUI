package com.github.stivais.aurora

// in 2.0.0, aurora will have standalone renderer in opengl,
// elements output vertices, which get inputted into a vbo
// at the moment I will focus on renderer for opengl 4.4 (or 4.5 IDK which one I need)

// i will also attempt to support states

class Aurora